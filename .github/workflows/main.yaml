name: Matrix Example

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: [3.9, 3.10, 3.11]

    steps:
      - name: Show Python version
        run: echo "Running on Python ${{ matrix.python-version }}"
